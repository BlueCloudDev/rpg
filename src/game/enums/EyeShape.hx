package game.enums;
enum EyeShape {
    // --- HUMAN / ANATOMICAL ---
    Almond;         // Classic oval shape, tapered ends
    Round;          // Large, open, often "innocent" or "doll-like"
    Monolid;        // Flat eyelid, no crease (East Asian characteristic)
    Hooded;         // Heavy upper lid, often looks brooding, intense, or aged
    DeepSet;        // Sunken into the skull, shadowed/mysterious
    Bulging;        // Protruding (Intense stare or fish-like features)
    Downturned;     // Outer corners dip low (Sad or weary look)
    Upturned;       // Outer corners lift high (Exotic or cat-like)

    // --- ANIMALISTIC / ALIEN ---
    VerticalSlit;   // Cat, Snake, or Dragon eyes (Predatory)
    HorizontalSlit; // Goat, Octopus, or Toad eyes (Prey/Alien)
    Compound;       // Faceted like a fly (Insectoids)
    Beady;          // Very small, shiny, black dots (Rodent-like)
    Stalked;        // Eyes on protruding stems (Snail or Crab-people)
    MultiLeveled;   // Multiple eyes arranged in a pattern (Spider)

    // --- ARTIFICIAL / CONSTRUCT ---
    Lens;           // A camera aperture (Shutter mechanism)
    SensorBar;      // A single strip of light (Cylon/Robocop style)
    PixelMatrix;    // A screen displaying digital eyes
    Bulb;           // A glowing glass sphere
    EmptySocket;    // No eye, just darkness (Undead or damaged)
}
